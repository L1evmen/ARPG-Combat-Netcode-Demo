namespace GameInventory
{
    /// <summary>
    /// 物品品质等级。
    /// </summary>
    public enum ItemRarity
    {
        Common,      // 凡品（白）
        Uncommon,    // 良品（绿）
        Rare,        // 上品（蓝）
        Epic,        // 特品（紫）
        Legendary    // 仙品（金）
    }
}
